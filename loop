/*
 * 반복문
 * for-in
 * for (integer in integers) {
    print(integer)
   }
 * for (name, age) in people {
   print("\(name): \(age)")   
 }
 *
 * while문
 * while *condition { }
 *
 * repeat-while
 * repeat {
    integers.removeLast()
 } while integers.count > 0
 */